from common import work, setup,smooth_scroll
import random, time

if __name__ == "__main__":
    # Only setup driver if selected
    driver = setup("CeliGunaw896")
    # smooth_scroll(driver)
    work(driver)